specVersion: "v1"
serviceId: "swiitchbank"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "swiitchbank-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
