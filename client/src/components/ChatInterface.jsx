import React from 'react';

const ChatInterface = ({ className }) => {
  return (
    <div className={className}>
      <h4>Original Chat Interface MVP Component</h4>
      <p>This is the user's chat interface for interacting with the AI.</p>
    </div>
  );
};

export default ChatInterface;
