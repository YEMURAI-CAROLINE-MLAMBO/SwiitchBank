import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  // This is a placeholder test.
  // We are not actually looking for a link,
  // just making sure the app renders without crashing.
  // A better test would be to look for a specific element.
});
