# Firebase Functions Configuration
# These are server-side credentials and should be kept secret.
# Do not commit the .env file with these values filled in.

# You can often get these from your Firebase project settings (Service Account JSON)
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# Other service keys that might be needed by functions
STRIPE_SECRET_KEY=
