# ==============================================================================
# SwiitchBank Monorepo - Master Environment Variables Template
# ==============================================================================
# This file serves as a master list of all environment variables required
# by the different services in this monorepo (backend, frontend, functions).
#
# INSTRUCTIONS:
# 1. DO NOT EDIT THIS FILE directly for your local setup.
# 2. For each service you are running, create a `.env` file in its directory
#    (e.g., `backend/.env`, `frontend/.env`).
# 3. Copy the RELEVANT variables from this master list into the service-specific
#    `.env` file and provide the appropriate values for your local environment.
# 4. `.env` files are ignored by Git and should NEVER be committed.
# ==============================================================================


# ------------------------------------------------------------------------------
# I. Backend Configuration (`backend/.env`)
# ------------------------------------------------------------------------------
# --- General ---
NODE_ENV=development
PORT=5001
CORS_ORIGIN=http://localhost:3000

# --- Database (MongoDB) ---
MONGODB_URI=mongodb://localhost:27017/swiitchbank
MONGODB_TEST_URI=mongodb://localhost:27017/swiitchbank_test

# --- Security ---
JWT_SECRET="YOUR_JWT_SECRET"
JWT_EXPIRES_IN=90d

# --- Third-Party API Keys ---
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"
STRIPE_SECRET_KEY="YOUR_STRIPE_SECRET_KEY"
MARQETA_API_KEY="YOUR_MARQETA_API_KEY"
MARQETA_API_URL="https://sandbox-api.marqeta.com/v3"
MOONPAY_API_KEY="YOUR_MOONPAY_API_KEY"


# ------------------------------------------------------------------------------
# II. Frontend Configuration (`frontend/.env`)
# ------------------------------------------------------------------------------
# Note: Create React App requires variables to be prefixed with `REACT_APP_`.
# --- API ---
REACT_APP_API_URL=http://localhost:5001

# --- Firebase (Client-Side) ---
REACT_APP_FIREBASE_API_KEY=your_firebase_api_key
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_project_id
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id
REACT_APP_FIREBASE_MEASUREMENT_ID=your_measurement_id


# ------------------------------------------------------------------------------
# III. Firebase Functions Configuration (`functions/.env`)
# ------------------------------------------------------------------------------
# --- Firebase Service Account (Server-Side) ---
# These are sensitive credentials for the Firebase Admin SDK.
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_PRIVATE_KEY="your_firebase_private_key_base64"
FIREBASE_CLIENT_EMAIL=your_firebase_client_email

# --- Third-Party API Keys (for Functions) ---
# Functions may need their own keys if they call services directly.
STRIPE_SECRET_KEY=your_stripe_secret_key