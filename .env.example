# SwiitchBank Monorepo Environment Variables
# -----------------------------------------------
# This is a master template of all environment variables used across the project.
# Create a .env file in each package (backend/, frontend/, functions/) and
# copy the relevant variables for that package.
# Do NOT commit .env files to version control.

# -- General Backend Configuration --
NODE_ENV=development
PORT=3000
LOG_LEVEL=info
JWT_SECRET=replace_with_strong_secret

# -- Database (PostgreSQL example) --
DATABASE_URL=postgres://user:pass@host:5432/dbname

# -- Frontend Configuration --
# The base URL for the backend API, used by the frontend.
API_BASE_URL=http://localhost:3000/api
# Example for a frontend-specific key, e.g., for Google Maps.
FRONTEND_API_KEY=""

# -- Firebase Configuration --
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_firebase_project_id
# For server-side services, you might use a private key (use with caution).
# Consider using Base64 encoding if you store it in a single line.
FIREBASE_PRIVATE_KEY=""

# -- Third-Party Services --

# Stripe
STRIPE_SECRET_KEY=your_stripe_secret_key

# Sentry (Error Reporting)
SENTRY_DSN=your_sentry_dsn

# Email Service (e.g., SendGrid, Mailgun)
MAILER_HOST=
MAILER_USER=
MAILER_PASS=
