export const InputSanitizer = {
  sanitizeInput: (input) => {
    // A simplified mock for testing purposes.
    // It just returns the input as is.
    return input;
  },
  sanitizeString: (str) => {
    return str;
  }
};