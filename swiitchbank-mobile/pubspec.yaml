name: swiitchbank
description: SwiitchBank Mobile - Anywhere Anytime Financial Management

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  shelf: ^1.4.1
  shelf_router: ^1.1.4


  cupertino_icons: ^1.0.2
  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/
    - .env
